{
  "name": "current-status-bot",
  "version": "1.0.0",
  "description": "This bot tweets a current status gif. It attempts to do so once every 20 minutes.",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Dory",
  "license": "ISC",
  "dependencies": {
    "node-base64-image": "^1.0.1",
    "requests": "^0.1.7",
    "twit": "^2.2.4"
  }
}
